import {lazy,Suspense} from 'react';
import {CTA,PageHero,SectionHeading} from '../components/UI';
import RetrievalLab from '../components/RetrievalLab';
const NetworkScene=lazy(()=>import('../components/NetworkScene'));
export default function Systems(){return <><PageHero eyebrow="SYSTEMS LAB / INTERACTIVE EXPLORATIONS" title={<>Open the<br/><span>black box.</span></>}><p>Rotate the architecture, select its layers, and run real retrieval against an editable knowledge base.</p></PageHero><section className="section no-top"><div className="lab-notice">REFERENCE ARCHITECTURE <span>Interactive concepts and synthetic documents. No proprietary client systems are connected.</span></div><div className="architecture-lab"><Suspense fallback={<div className="scene-loading">Loading the 3D architecture…</div>}><NetworkScene expanded/></Suspense><div className="layer-panel"><span className="kicker">ENGINEERING THE FULL WORKFLOW</span><h2>Every layer<br/>has a purpose.</h2><p>Select a layer to inspect its role. The architecture connects data readiness, relevant context, model inference, controlled action, and quality feedback.</p><div className="three-grid stacked-cards">{[['Trust the answer','Evaluate retrieval and generation independently.'],['Control the action','Validate permissions, inputs, and workflow state.'],['Operate the system','Version changes, monitor regressions, and enable rollback.']].map(([a,b])=><article key={a}><h3>{a}</h3><p>{b}</p></article>)}</div></div></div></section><section id="workflow" className="section light-section"><SectionHeading eyebrow="LIVE RETRIEVAL WORKBENCH" title={<>Your query.<br/>Actual ranked results.</>}><p>Edit documents, tune BM25, inspect scores, and label relevance. All computation runs in your browser.</p></SectionHeading><RetrievalLab/></section><CTA/></>}
