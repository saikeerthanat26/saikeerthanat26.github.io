import {useState} from 'react';
import {AnimatePresence} from 'motion/react';
import {Link} from 'react-router-dom';
import projects from '../data/projects.json';
import ProjectCard from '../components/ProjectCard';
import {CTA,PageHero,SectionHeading} from '../components/UI';
export default function Work(){const [filter,setFilter]=useState('All');const filtered=projects.filter(p=>filter==='All'||p.industry===filter);return <><PageHero eyebrow="SELECTED WORK / 2016—PRESENT" title={<>The engineering<br/>behind the <span>outcome.</span></>}><p>Explore three detailed case studies through the lens of your business.</p></PageHero><section className="section no-top"><div className="filters" role="group" aria-label="Project industries">{['All','Healthcare','Finance','Retail & search'].map(x=><button key={x} aria-pressed={x===filter} onClick={()=>setFilter(x)}>{x==='All'?'All work':x}</button>)}</div><p className="fine" role="status">{filtered.length} case {filtered.length===1?'study':'studies'}</p><div className="project-grid filtered-grid"><AnimatePresence>{filtered.map(p=><ProjectCard key={p.slug} project={p} index={projects.indexOf(p)}/>)}</AnimatePresence></div><p className="fine">Metrics come from my résumé. Client names identify engagements, not endorsements.</p></section><section className="section light-section"><SectionHeading eyebrow="MORE ENTERPRISE EXPERIENCE" title="A broader foundation."><Link className="text-link" to="/experience">Full career & skills ↗</Link></SectionHeading><div className="three-grid">{[['State of Wisconsin','Demand forecasting, resource allocation, and reliable data pipelines.'],['Costco Wholesale','Enterprise knowledge retrieval and retail analytics.'],['Goldman Sachs','Document intelligence for legal and compliance workflows.']].map(([title,text])=><article key={title}><h3>{title}</h3><p>{text}</p></article>)}</div></section><CTA/></>}
